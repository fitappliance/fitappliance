name: Sync Appliance Data (Retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement path
        run: |
          echo "This workflow is retired and intentionally performs no data mutation."
          echo "Retailer inputs must enter Architecture V2 as typed observations with immutable acquisition receipts."
          echo "Released runtime output is produced only by the canonical build in a reviewed pull request."
